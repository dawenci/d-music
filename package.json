{
  "scripts": {
    "precommit": "lerna run --concurrency 1 --stream precommit"
  },
  "license": "MIT",
  "devDependencies": {
    "husky": "^0.14.3",
    "lerna": "^2.8.0",
    "lint-staged": "^7.0.0",
    "prettier": "^1.11.0"
  }
}
